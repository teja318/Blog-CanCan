<h2> Listing users </h2>
<% @users.each do |user| %>
	<li><%= user.email %> - <%= user.roles.pluck(:name).join(",") %> </li>
	<% end %>
	<% if can? :create, User.new %>
	<%= link_to "add users", new_user_path %>
	<% end %>